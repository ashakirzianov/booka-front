export type TextCallbacks = {
    onClick: void,
};
export type TextProps = {
    style: {
        fontFamily?: string,
        fontSize?: number,
        textAlign?: 'justify',
        color?: string,
    },
};
