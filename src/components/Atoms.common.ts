export type TextCallbacks = {
    onClick: undefined,
};
export type TextProps = {
    style: {
        fontFamily?: string,
        fontSize?: number,
        textAlign?: 'justify',
        color?: string,
    },
};
